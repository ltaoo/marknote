import React, {Component} from 'react'

export default class Tree extends Component {
    constructor(props) {
        super(props)
    }

    _renderTree() {
        // 渲染树
        const {tree} = this.props
        // 对树进行遍历
        
    }

    render() {
        return (
            <ul>
        )
    }

}